# Issue 1
# If there are multiple subnets in dhcpd.conf file and all the subnets have option routers set,
# then the most recently renewed interface will overwrite the default Gateway
option routers <GATEWAY>
