# To ssh via pem file (which normally needs 0600 permissions):
ssh -i /path/to/file.pem user@example.com

# To connect on an non-standard port:
ssh -p 2222 user@example.com

<<<<<<< HEAD
# To execute a command on a remote server, Force pseudo-tty allocation.  This can be used # to execute arbitrary screen-based programs on a remote machine, which can be very # useful, e.g. when implementing menu services. Multiple -t options force tty allocation, even if ssh has no local tty.
=======
# To connect and forward the authentication agent
ssh -A user@example.com

# To execute a command on a remote server:
>>>>>>> 03bb651bf0c2a507799d0662d237f6b11c3cc053
ssh -t user@example.com 'the-remote-command'

# Disable pseudo-tty allocation.
ssh -T user@example.com

# To tunnel an x session over SSH:
ssh -X user@example.com

# To launch a specific x application over SSH:
ssh -X -t user@example.com 'chromium-browser'

# To create a SOCKS proxy on localhost and port 9999
ssh -D 9999 user@example.com

# -X use an xsession, -C compress data, "-c blowfish" use the encryption blowfish
ssh user@example.com -C -c blowfish -X

# For more information, see:
# http://unix.stackexchange.com/q/12755/44856
